# requirements.txt

# Core — use contrib build so basicsr/albumentations opencv requirement is satisfied
# (opencv-contrib-python is a strict superset of opencv-python; provides same cv2)
opencv-contrib-python>=4.9.0.80
numpy==1.26.4
Pillow==10.3.0
scipy==1.13.0

# Face Detection & Landmarks
insightface==0.7.3
onnxruntime>=1.20.0          # CPU version (must be >=1.20 for NumPy 2.x compat)
# onnxruntime-gpu>=1.20.0    # Uncomment for GPU
mediapipe==0.10.14

# Segmentation & Restoration
torch==2.3.0
torchvision==0.18.0
# For GPU: torch==2.3.0+cu121
scikit-image==0.23.2
albumentations>=2.0.0        # pin 2.x — older versions require opencv-contrib separately
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
realesrgan==0.3.0

# Web server
flask==3.0.3
flask-cors>=4.0.0

# High-quality hair transfer via hosted HairFastGAN Space (optional)
gradio_client>=1.3.0,<2.0.0

# Utils
tqdm==4.66.4
pyyaml==6.0.1
python-dotenv==1.0.1
requests==2.32.2
protobuf>=4.25.3,<6.0
