# requirements-deploy.txt  (CPU-only cloud deployment)
# NOTE: torch + torchvision are installed separately in Dockerfile
#       to avoid index-url conflicts with PyPI packages.

# Core web
flask==3.0.3
flask-cors==4.0.1

# Image processing
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.3.0
scipy==1.13.0
scikit-image==0.23.2
imageio==2.34.1

# Face detection & landmarks
insightface==0.7.3
onnxruntime==1.19.2
# mediapipe omitted — linux_x86_64 wheel incompatible with HF Spaces glibc.
# landmarks.py falls back to OpenCV Haar cascade automatically.

# Face restoration (fixes the 128x128 swap blur). Runs on CPU here — slower
# than GPU but only restores 512px face crops, so it's tolerable. basicsr
# imports torchvision.transforms.functional_tensor (removed in tv>=0.17); the
# shim at the top of core/super_res.py recreates it so these import cleanly.
# RealESRGAN is installed for the API but only used on GPU; on CPU super_res
# falls back to a Lanczos upscale to avoid request timeouts.
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
realesrgan==0.3.0

# Utils
python-dotenv==1.0.1
requests==2.32.2
tqdm==4.66.4
pyyaml==6.0.1
protobuf>=3.20,<4.0
